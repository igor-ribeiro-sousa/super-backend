spring:
  application:
    name: api-rest
    
  config:
    import: application-environment.yml

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  servlet:
    context-path: /api
    
  port: 8081